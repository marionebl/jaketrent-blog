<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: apache | Jake Trent]]></title>
  <link href="http://jaketrent.com/blog/categories/apache/atom.xml" rel="self"/>
  <link href="http://jaketrent.com/"/>
  <updated>2016-12-15T09:53:49-07:00</updated>
  <id>http://jaketrent.com/</id>
  <author>
    <name><![CDATA[Jake Trent]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Running Php in Apache on Linux]]></title>
    <link href="http://jaketrent.com/post/running-php-apache-linux/"/>
    <updated>2008-10-15T14:15:00-06:00</updated>
    <id>http://jaketrent.com/post/running-php-apache-linux</id>
    <content type="html"><![CDATA[<p>I haven't done php for a while, but at work our prototypes are in html/php.  So, I needed to get Apache webserver downloaded and working on my Linux box with php.  Here's how it all went down.</p>

<!--more-->


<p>I haven't done php for a while, but at work our prototypes are in html/php.  So, I needed to get Apache webserver downloaded and working on my Linux box with php.  Here's how it all went down:</p>

<p>Install apache2 and the php handler:</p>

<p><code>bash
sudo apt-get install apache2 libapache2-mod-php5
</code></p>

<p>Find the DocumentRoot:</p>

<p><code>bash
sudo emacs /etc/apache2/sites-available/default
</code></p>

<p>Mine defaulted to /var/www/.  I have a limited number of my projects on this box right now, so I decided to setup sym links to the project directories:</p>

<p><code>bash
cd /var/www/;  ln -s ~/dev/records-proto/ proto
</code></p>

<p>Now I need to tell apache that I want my .html files treated as php files too (because that's how our designer did it).  I did a grep on 'AddType':</p>

<p><code>bash
cd /etc/apache2/; grep AddType * -r
</code></p>

<p>That turned up the file that you need to edit:
<code>bash
sudo emacs mods-enabled/php.conf &amp;
</code></p>

<p>I made the php handler line look like this (added the .html on the end):
<code>xml
AddType application/x-httpd-php .php .phtml .php3 .html
</code></p>

<p>Now the server's going to need a restart:
<code>bash
sudo /etc/init.d/apache2 restart
</code></p>

<p>Now, I point my favorite web browser to this address (using the sym-link), and I'm good to go:
<code>bash
firefox http://localhost/proto &amp;
</code></p>
]]></content>
  </entry>
  
</feed>
